# Week 7 Requirement Traceability Matrix — v1.1

> Baseline date: 13 July 2026
>
> Controlled update: 25 August 2026
>
> Decision source: [Week 7 Client Decision Log](../decisions/client-decision-log-v1.md)
>
> Matrix owner: Zhaohang (Documentation/Client)
>
> Matrix reviewer: Lin (Modelling/Evaluation)

## 1. Usage Rules

- Every requirement must trace to a `DEC-*` entry; a decision may be marked `Verified` only after all acceptance criteria pass.
- Status values are restricted to `Not started`, `Partial`, `Blocked`, and `Verified`.
- An item must not be marked `Verified` without reproducible test evidence or PR/commit evidence.
- `Planned` test evidence identifies an intended artefact and does not mean that the test has passed.
- Owners and reviewers follow the Week 7 role allocation, and the reviewer must differ from the owner.

### Week 7 Role Allocation

| Role | Main responsibility this week | Owner | Reviewer |
|---|---|---|---|
| Architecture/State | Goal configuration, dependencies, states, and stale rules | Bo | Lin |
| Data Pipeline | Data dictionary, templates, validation, and HDE/RHS schema | Di | Benyu |
| UX/Workflow | Five-stage navigation, Goal Selector, and checkpoint prototype | Lin | Bo |
| Modelling/Evaluation | Filtering, exclusion log, model interface, and experiment design | Yutong | Di |
| QA/Reproducibility | Testing, error recovery, fixtures, and regression verification | Benyu | Bo |
| Documentation/Client | Decision log, traceability matrix, meeting evidence, and ethics materials | Zhaohang | Yutong |

### Report Destination Key

- `Group — Requirements/Data Design`: requirements, data-contract, and design sections of the group report.
- `Group — Implementation/Testing`: implementation and testing sections of the group report.
- `Individual — Modelling/Evaluation`: modelling, filtering, and evaluation thread of the individual report.
- `Journal — Workflow/Provenance`: task-oriented workflow, reliability, and provenance evidence for the journal paper.

## 2. Traceability Matrix

| RTM ID | Decision | User Goal / Stage | Requirement | Priority | Owner | Reviewer | Acceptance Criteria | Test Evidence | Report Destination | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| RTM-01 | DEC-39 | Stage 1 — Prepare Sites and Data | Use five separate user-facing CSV routes for Biology, Environmental/Site, daily Flow, WQ, and RHS. Do not expose an XLSX template, upload, checkpoint, or standalone validation route. | Must | Di | Benyu | Each data view downloads one valid example matching the exact `DC-13` source schema, rejects missing/unexpected/reordered fields at upload, performs the documented one-way canonical conversion, and routes valid local data into the same downstream boundary as its external equivalent. Environmental optional-column behaviour follows `OPEN-12`. Existing XLSX code is restricted to internal historical migration. | Existing partial evidence: individual local CSV controls and shared validation helpers exist, but a user-facing DC-11 validation sandbox remains.<br>Planned: five source-schema, canonical-conversion, active-routing, and no-XLSX-route tests. | Group — Requirements/Data Design; Group — Implementation/Testing | Partial |
| RTM-02 | DEC-02 | Stages 1–2 — Biology import/validation | Biology data must contain at least one supported index but must not require both `WHPT_ASPT` and `LIFE_F`. | Must | Di | Benyu | Valid data containing only `WHPT_ASPT` or only `LIFE_F` pass; data with all supported index fields absent are blocked and the accepted fields are listed. | Planned: `tests/test_biology_schema.R`, covering one index, multiple indices, and no supported index. | Group — Requirements/Data Design; Group — Implementation/Testing | Not started |
| RTM-03 | DEC-38 | Stages 2–3–5 — Flow processing, join, and model | Joined data contain Q10/Q95 raw and Z-score fields for lags 0, 1, 3, 6, and 12, with matching per-lag Flow-window provenance. Q50 remains descriptive only. | Must | Di | Benyu | UI and server accept exactly the five supported lags; joined output contains all twenty fixed-order Q fields plus start/end/inclusive-duration provenance; unselected lag fields are typed `NA`; no internal window lookup key or Q50 model option leaks into user output. | Current `main` accepts only lag 0/1.<br>Planned: join-contract, server-bypass, canonical-order, and provenance tests for all five lags. | Group — Requirements/Data Design; Individual — Modelling/Evaluation | Partial |
| RTM-04 | DEC-04 | Stage 1 — WQ schema | Remove `area` from the Dashboard schema, template, standardised data, and model selector. | Must | Di | Benyu | Downloaded templates and standardised WQ data do not contain `area`; an uploaded extra `area` field is not used for joining, analysis, or modelling and is identified as unsupported in the schema report. | Existing partial evidence: the current workbook reader does not declare `area`.<br>Planned: `tests/test_wq_schema.R` and a template-field check. | Group — Requirements/Data Design | Partial |
| RTM-05 | DEC-05, DEC-24 | Stage 3 — WQ enrichment | For a biology record with `sampling_year = Y`, v1 uses the inclusive calendar-year window `Y - 2` through `Y`; `0180` orthophosphate uses the mean, `0111` ammonia uses P90, and dissolved oxygen uses P10 but remains `not_ready` until `OPEN-02` is closed. | Must | Di | Benyu | Boundary fixtures include records on 1 January of `Y - 2` and 31 December of `Y`, exclude records outside those calendar years, and produce `orthophosphate_mean` and `ammonia_p90` values matching independent R calculations. The dissolved-oxygen branch rejects guessed values while its determinand is unresolved; every enriched record stores window start/end/duration, aggregation method, and record count. | Planned: `tests/test_wq_enrichment.R` with boundary/known-result fixtures and an unresolved dissolved-oxygen guard. | Group — Implementation/Testing; Journal — Workflow/Provenance | Not started |
| RTM-06 | DEC-06, DEC-24 | Stages 2–3 — WQ cleaning/enrichment | Transform WQ observations below the detection limit using `value / 2`, while retaining the original value, unit, qualifier, and transformation provenance. | Must | Di | Benyu | When the qualifier indicates a below-detection observation, the analysis value equals half the reported value; ordinary observations remain unchanged; downloads and provenance preserve source result, source unit, qualifier, and transformation reason. | Planned: `tests/test_wq_detection_limit.R`, covering `<`, ordinary values, missing qualifiers, and missing values. | Group — Implementation/Testing; Journal — Workflow/Provenance | Not started |
| RTM-07 | DEC-07 | Stage 4 — Explore and Refine | Site/sample filtering creates only a new `analysis_dataset` and `exclusion_log`; it must not change upstream O:E, flow statistics, `joined_core`, or `joined_enriched`. | Must | Lin | Di | Upstream dataset contents and row counts remain identical before and after filtering; analysis rows change as selected; every exclusion stores an ID, reason, trigger, and timestamp; restoring a record rebuilds the analysis dataset and marks the model stale. | Planned: `tests/test_analysis_filtering.R` and a state regression test. | Individual — Modelling/Evaluation; Journal — Workflow/Provenance | Not started |
| RTM-08A | DEC-08 | Stage 5 — Single-site Model and Export | Route one-site data to the single-site model path; the final model engine and smooth/interaction contract remain governed by `OPEN-08`. | Must | Lin | Di | The single-site path must not be marked finally verified until the team reconciles the existing additive implementation with the client's `mgcv::gam()` guidance and freezes formula, smoothing, interaction, diagnostic, comparison, and export rules. | Existing `lm()`/additive helpers are provisional evidence only; `SRC-09` proposes `mgcv::gam()` for both paths.<br>Planned after `OPEN-08`: engine-specific numerical parity and end-to-end model refinement tests. | Individual — Modelling/Evaluation; Group — Implementation/Testing | Blocked |
| RTM-08B | DEC-08 | Stage 5 — Multi-site Model and Export | Route multi-site data to the multiple-site model path; the existing mixed-effects contract remains a tested implementation baseline, not the final client model-engine decision. | Must | Lin | Di | The path must not be marked finally verified until `OPEN-08` reconciles the existing `lmer` contract with the client's GAM random-effect/smooth examples and freezes permitted formulas, structures, diagnostics, model comparison, and exports. | Existing evidence: [`mixed_model_helpers.R`](../../R/mixed_model_helpers.R), [`analysis_model_helpers.R`](../../R/analysis_model_helpers.R), and parity tests. This evidence remains useful but cannot resolve the later GAM requirement.<br>Planned after `OPEN-08`: frozen-engine sufficiency, failure-handling, diagnostic, comparison, and numerical-parity tests. | Individual — Modelling/Evaluation; Group — Implementation/Testing | Blocked |
| RTM-09 | DEC-09 | Stage 5 — Predictor selection and sampling year | V1 permits up to two flow predictors, one WQ predictor, and one RHS predictor; calculate `sampling_year_centered = sampling_year - (min_year + max_year) / 2`. When `sampling_year` contains at least two distinct valid years, applicable single-site and multi-site model formulas must include `sampling_year_centered` by default. | Must | Lin | Di | Both UI and server reject selections above each category limit, and centred values match an independent calculation. With at least two valid years, the fitted formula must contain `sampling_year_centered`. A missing `sampling_year` column, all-blank values, or unparseable years block execution with a field-level error. Partial missingness uses complete-case exclusion and reports the count. With a constant year, the system must not fit a year coefficient; it displays a warning, explicitly removes the term, and records the reason in provenance. Formula, centring reference, valid-year range, excluded-record count, and year-term status are included in downloads and provenance. | Existing partial evidence: the current basic model accepts one flow predictor but does not verify that centred year enters the fitted formula.<br>Planned: `tests/test_predictor_constraints.R`, covering normal multi-year data, partial missingness, a missing field, invalid years, a constant year, and formula inclusion of `sampling_year_centered`. | Individual — Modelling/Evaluation | Partial |
| RTM-10 | DEC-10 | Stage 5 — Mixed-effects configuration | Mixed models permit only a random intercept or a random slope using midpoint-centred sampling year. | Must | Lin | Di | The UI displays only `(1 \| biol_site_id)` and `(sampling_year_centered \| biol_site_id)`; server-side submission of another grouping/random structure is rejected with an actionable message. | Planned: `tests/test_mixed_effect_structure.R`. | Individual — Modelling/Evaluation; Group — Implementation/Testing | Not started |
| RTM-11 | DEC-11 | Stages 1–3 — RHS import/enrichment | Use `rhs_survey_id` as the sole canonical RHS identifier and treat RHS as site-level enrichment. Reject `rhs_site_id` in Dashboard mapping input, never convert it silently, and retain only `rhs_survey_id` in internal data and outputs. An external RHS `Survey.ID` may be explicitly renamed once at the ingestion boundary. | Must | Di | Benyu | A mapping containing only `rhs_survey_id` passes and completes RHS mapping. A mapping containing only `rhs_site_id` fails with an instruction to use `rhs_survey_id`. A mapping containing both fields fails with an instruction to remove `rhs_site_id`, whether their values match or differ. An external record containing `Survey.ID` is explicitly converted to `rhs_survey_id` by the ingestion adapter, after which `Survey.ID` is absent. Internal data, mappings, model inputs, provenance, downloads, templates, and generated outputs contain `rhs_survey_id` and never contain or generate `rhs_site_id` as an alias. | Implemented evidence: [`site_mapping_helpers.R`](../../R/site_mapping_helpers.R) rejects legacy and dual-field inputs, permits the explicit external `Survey.ID` boundary conversion, and retains only `rhs_survey_id`; [`dashboard_backlog_helpers.R`](../../R/dashboard_backlog_helpers.R), UI/server paths, templates, and fixtures use the canonical field. [`test_site_mapping.R`](../../tests/test_site_mapping.R) covers canonical-only, legacy-only, matching/conflicting dual-field, external ingestion, and no-alias cases; all current automated tests pass.<br>Historical evidence: [`FT-05A`](../testing/reports/2026-07-10/Functional_Test_Execution_Record.md) records the superseded compatibility workaround. Full model/provenance/download verification remains pending. | Group — Requirements/Data Design; Group — Implementation/Testing | Partial |
| RTM-12 | DEC-12 | Stages 1–2 — Flow import | HDE is the default flow source; use NRFA only when HDE has no coverage or fails, and record the fallback reason and actual source. | Must | Di | Benyu | When no source is specified, HDE is attempted first; NRFA is called only for a recognised coverage/failure condition; success, fallback, and double failure each produce structured provenance and a user-facing message. | Existing partial evidence: current code accepts HDE/NRFA, but demo data and fixtures default to NRFA and there is no automatic fallback provenance; [`FT-03A`](../testing/reports/2026-07-10/Functional_Test_Execution_Record.md) records the failed NRFA assumption.<br>Planned: `tests/test_flow_source_fallback.R` | Group — Requirements/Data Design; Journal — Workflow/Provenance | Partial |
| RTM-13 | DEC-13 | QA — WQ performance | Normal WQ tests use fewer than 10 sites; maintain fixed 5/10/20/49-site fixtures, with 49 sites used only for stress/performance testing. | Must | Benyu | Bo | Seed `20260713` reproduces the same four site-ID sets; 5 sites represent normal use, 10/20 sites are extended performance cases, and 49-site results do not affect ordinary usability pass/fail. | Existing partial evidence: [`FT-04A`](../testing/reports/2026-07-10/Functional_Test_Execution_Record.md) records approximately 20 minutes for 49 sites, but fixed tiered fixtures do not exist.<br>Planned: `tests/fixtures/wq_sites_{5,10,20,49}.csv` and a performance log | Group — Implementation/Testing; Journal — Workflow/Provenance | Partial |
| RTM-14 | DEC-14, DEC-39 | Stage 1 and Export — Local binding | Bind external and local data only after both sources satisfy the same versioned per-type CSV schema; expose final selected/bound data, not import intermediates. | Must | Di | Benyu | Missing, extra, duplicated, or reordered fields block binding and are listed. Valid sources preserve every input row and source label until the conflict boundary in RTM-26 is resolved. Downloads contain only the final selected/bound dataset and provenance. | Existing exact-order validation and `bind_rows()` behaviour are partial evidence.<br>Planned: five per-type binding tests and final-download surface checks. | Group — Requirements/Data Design; Group — Implementation/Testing | Partial |
| RTM-15 | DEC-15 | Stage 1 — Local flow upload | Local `flow_daily` contains only `flow_site_id`, `date`, and `flow`; `flow_input` belongs only to `site_mapping`. | Must | Di | Benyu | A three-field flow file validates successfully; absence of any required field blocks continuation; the local-flow validator does not require `flow_input`; external-import mapping still validates HDE/NRFA source. | Existing partial/conflicting evidence: [`read_he_dashboard_template.R`](../design/data-template/read_he_dashboard_template.R) uses the three-field schema, but current [`local_flow.csv`](../../tests/fixtures/local_flow.csv) and helper still require `flow_input`.<br>Planned: update fixture and validator tests | Group — Requirements/Data Design; Group — Implementation/Testing | Partial |
| RTM-16 | DEC-16 | Stages 1–2 — Biology/O:E | Users do not upload O:E; the Dashboard calculates O:E after biology/environment validation and processing. | Must | Di | Benyu | The template exposes no O:E input field; an uploaded O:E field is identified as unsupported and is not treated as a calculated result; valid processed inputs produce a new O:E value with provenance. | Existing partial evidence: [`FT-06A/FT-06B`](../testing/reports/2026-07-10/Functional_Test_Execution_Record.md) show that the Dashboard performs RICT/O:E, but the new upload contract lacks regression verification.<br>Planned: `tests/test_oe_input_contract.R` | Group — Requirements/Data Design; Journal — Workflow/Provenance | Partial |
| RTM-17 | DEC-17, DEC-48 | Stages 1–2 — Environmental validation | Require user-facing `NGR_10_FIG` and derive exact internal `NGR_PREFIX` at ingestion; when `ALKALINITY` is blank, at least one of `CONDUCTIVITY`, `TOTAL_HARDNESS`, or `CALCIUM` must support proxy estimation. | Must | Di | Benyu | Missing or unparseable `NGR_10_FIG` blocks conversion with a field-level error; valid input produces internal `NGR_PREFIX` without retaining `NGR_prefix` or requiring a second user field. A valid alkalinity passes; blank alkalinity with at least one valid proxy passes and displays a proxy checkpoint; all four blank values block `predict_indices()`; units are shown for every input. | Existing map parsing of `NGR_10_FIG` is partial evidence, but the formal ingestion conversion is not complete.<br>Planned: `tests/test_environmental_validation.R`, covering grid parsing, source-to-canonical naming, all four chemistry branches, and unit metadata. | Group — Requirements/Data Design; Group — Implementation/Testing | Not started |
| RTM-18 | DEC-18, DEC-24, DEC-42 | Stage 1 — WQ/RHS schema and mapping | WQ observations require a four-character `det_id`; Flow, WQ, and RHS coordinate fields are prohibited from formal user inputs and standardised outputs. | Must | Di | Benyu | WQ data without `det_id` are rejected; `180`/`"180"`/`"0180"` normalise to `"0180"` while preserving the source representation; WQ/RHS observations and mappings contain no non-Biology coordinates; identifier types and explicit site relationships remain intact. | Current WQ normalisation and mapping are partial evidence; the merged Stage 1 map still exposes non-Biology coordinates.<br>Planned: schema, prohibited-field, mapping, and output tests. | Group — Requirements/Data Design; Group — Implementation/Testing | Partial |
| RTM-19 | DEC-19 | Stage 3 — Build HE Dataset | Build `joined_core` first, then optionally build `joined_enriched`; a WQ/RHS enrichment failure must not invalidate the core. | Must | Di | Benyu | A successful core join records an independent complete state; exploration remains available without WQ/RHS; an enrichment failure leaves `joined_core` content and state unchanged and offers warning/retry; success creates a separate `joined_enriched`. | Existing partial evidence: a biology-flow join path exists, but [`ST-05A`](../testing/reports/2026-07-10/Smoke_test_execution_2026-07-10.md) fails because of BUG-001; independent core/enriched state does not exist.<br>Planned: `tests/test_core_enriched_state.R` | Group — Requirements/Data Design; Journal — Workflow/Provenance | Partial |
| RTM-20 | DEC-20 | Stages 1–3–5 — RHS field contract | Apply one-way standardisation from legacy `HMS.Score` to `HMSRBB`, make `HMSRBB` available as a controlled RHS predictor, and permit only `HMSRBB` in internal data and outputs. | Must | Di | Benyu | With only `HMS.Score`, the system accepts the input, renames it once to `HMSRBB`, and displays a compatibility warning. With only `HMSRBB`, it accepts the input without that warning. If both fields exist with identical values, it retains `HMSRBB`, removes the legacy field, and displays a duplicate-compatibility warning. Conflicting values block continuation. Internal data, template, joined output, model selector, downloads, and model provenance must not contain `HMS.Score`; provenance records whether the legacy rename occurred but never re-emits the legacy field. | Planned: `tests/test_hmsrbb_contract.R`, covering legacy-only, new-only, matching dual fields, conflicting dual fields, and absence of `HMS.Score` from every output and the model selector. | Group — Requirements/Data Design; Individual — Modelling/Evaluation | Not started |
| RTM-21 | DEC-40 | Stage 5 — Model-variable eligibility | Both model paths may use Q10z/Q95z lag fields; single-site models may additionally use raw Q95; multi-site models reject every raw Flow predictor. | Must | Lin | Di | The selector exposes exactly the eligible fields for the detected model path and all five lags. Server-side submissions of raw Q10 or any raw multi-site Flow field are rejected with an actionable standardisation message. | Existing model-path validation is partial evidence.<br>Planned: selector and server-bypass tests for single-site and multi-site datasets. | Individual — Modelling/Evaluation; Group — Implementation/Testing | Partial |
| RTM-22 | DEC-42 | Stage 1 — Site mapping | The map and user-facing mapping contract retain Biology coordinates only; Flow, WQ, and RHS remain identifier mappings without coordinate fields. | Must | Di | Benyu | Templates, validators, UI controls, map layers, standardised outputs, and downloads contain no Flow/WQ/RHS coordinate fields. Biology markers render when valid coordinates exist, while mappings still require explicit supporting-site IDs and never infer relationships from equal text. | Current `main` displays Biology, Flow, and WQ markers and therefore conflicts with the latest confirmation.<br>Planned: prohibited-field, map-layer, and mapping-regression tests. | Group — Requirements/Data Design; Journal — Workflow/Provenance | Not started |
| RTM-23 | DEC-23 | Stages 2–4 — Biology quality/refinement | Multiple biology samples at the same site on the same date or in the same month-year produce a warning/flag only; they are not automatically rejected, deleted, or aggregated. User-requested aggregation records its method. | Must | Di | Benyu | Same-date and same-month fixtures retain all source rows and receive distinct flags; continuation remains available with a checkpoint; aggregation provenance records method, input count, and output count. | Planned: `tests/test_biology_duplicates.R`. | Group — Implementation/Testing; Individual — Modelling/Evaluation; Journal — Workflow/Provenance | Not started |
| RTM-24 | DEC-24 | Stages 1–3 — WQ determinand registry and unit normalisation | `0180`/`0111` use the frozen canonical names, `mg/L` unit, and summary roles; `0119` is not an ammonia alias; package unit aliases normalise while preserving source/provenance. | Must | Di | Benyu | `0180` outputs `Orthophosphate reactive as P`, and `0111` outputs `Ammoniacal Nitrogen as N`; `mg/L`, `mg/l`, and `MILLIGRAM PER LITRE` all output `mg/L`; `0119` never enters `ammonia_p90`; ID/text conflicts and unsupported units produce field-level errors; downloads restore source unit/value/qualifier. | Planned: `tests/test_wq_determinand_registry.R`, covering ID padding, display aliases, unit aliases, `0119` exclusion, ID/text conflicts, and provenance. | Group — Requirements/Data Design; Group — Implementation/Testing; Journal — Workflow/Provenance | Not started |
| RTM-25 | DEC-37 | Stage 4 — Generate HEV plots | A user must be able to generate an HEV plot from either current raw daily Flow or current time-varying Flow Statistics generated by `calc_flowstats()`, selected explicitly for each request. Raw-daily mode must not require Flow Statistics, and the Dashboard must not silently substitute modes. | Must | Di | Benyu | The HEV UI exposes `daily_flow` and `flow_statistics` choices. With valid mapped biology/O:E and daily Flow but no current Flow Statistics, `daily_flow` generates a plot from the canonical numeric `flow` field. With current `calc_flowstats()` output, `flow_statistics` generates a plot from the selected statistic. Missing or stale data in one mode blocks that request without using the other mode or overwriting a previous valid result. Switching mode or changing either source marks the current plot stale. On-page provenance and download history identify mode, source revision/fingerprint, mapped site IDs, date range, and Flow field/statistic. | Implemented evidence: [`hev_output_helpers.R`](../../R/hev_output_helpers.R), the mode selector and server path, and HEV mode/state cases in [`test-workflow-server.R`](../../tests/testthat/test-workflow-server.R). Final manual HEV output verification against client data remains required. | Group — Requirements/Data Design; Group — Implementation/Testing; Journal — Workflow/Provenance | Partial |
| RTM-26 | DEC-41 | Stage 1 — Combine local and external data | Combined sources must retain source identity, flag exact duplicates and conflicting records, and require an explicit user source choice for conflicts without silent override, deletion, or deduplication. | Must | Di | Benyu | Exact duplicates remain present and flagged. Conflicts display both candidate records and block downstream use until a valid source choice is recorded. The result records comparison key, candidates, choice, timestamp, and before/after counts; changing the choice rebuilds downstream data without mutating either input source. | No conforming combined-source resolver exists on current `main`.<br>Planned: identity, conflict, explicit-choice, no-data-loss, provenance, and stale-propagation tests for each eligible source type. | Group — Requirements/Data Design; Group — Implementation/Testing; Journal — Workflow/Provenance | Partial |
| RTM-27 | DEC-43 | Task selection and navigation | Tasks 1-5 stop after Stages 2, 2, 3, 4, and 5 respectively; later Stages are disabled, and Task 1 exposes only Biology and Environmental/Site imports. | Must | Lin | Bo | Workflow configuration, navigation, Stage guidance, Start/Resume, and direct navigation all use the exact five paths. A disabled Stage cannot be opened by UI or server bypass. Task 1 does not render Flow, WQ, or RHS import actions. | Current path configuration contains superseded optional later Stages.<br>Planned: exact matrix, navigation, bypass, and Task 1 visibility tests. | Group — Requirements/Data Design; Group — Implementation/Testing | Partial |
| RTM-28 | DEC-44 | Stage 2 — WQ/RHS processing and preview | Build WQ Summary belongs to Stage 2; WQ dates start at 2000-01-01; WQ preview uses determinand/site controls with fixed date/result axes and time-series/boxplot choices; RHS preview plots are absent. | Must | Di | Benyu | Dates before 2000-01-01 are unavailable and rejected server-side. The preview appears before the summary, toggles with results, requires a determinand, groups by `wq_site_id`, exposes no arbitrary numeric/x-axis selectors or bar chart, and the RHS view has no preview plot action. Lin implements the corresponding UI placement and controls. | Existing WQ helpers and date-floor logic are partial evidence; the confirmed UI and Stage placement are incomplete.<br>Planned: helper, server, UI visibility, and manual plot checks. | Group — Requirements/Data Design; Group — Implementation/Testing | Partial |
| RTM-29 | DEC-46 | Stage 1 — Biology site map | Biology-only map popups show the available Biology site ID, water-body name, sample count, and sample-year range without requiring every optional metadata field. | Must | Lin | Bo | Valid Biology coordinates always render one marker. Complete metadata produces all four popup elements; missing optional metadata produces explicit unavailable values without hiding the marker. Values are escaped and no Flow/WQ/RHS layer appears. Di supplies canonical fields and fallback semantics. | No complete popup-contract evidence is recorded.<br>Planned: complete/partial metadata, escaping, and Biology-only layer tests. | Group — Implementation/Testing; Journal — Workflow/Provenance | Not started |
| RTM-30 | DEC-47 | Cross-stage operational usability | Provide the exact one-site PCA message, working Flow heatmaps, informative long-task progress, responsive full-width content, legible/navigable tables and plots, and appropriate three-significant-figure model output. | Must | Lin | Bo | Automated tests cover the PCA message, Flow heatmap rendering/data, and numeric formatting. Reproducible manual checks cover progress feedback, desktop/mobile width, table navigation, clipping, and plot legibility without console errors. Benyu records QA evidence. | Individual components exist, but the complete client acceptance set has not been demonstrated.<br>Planned: targeted regression tests and a viewport/manual QA record. | Group — Implementation/Testing | Partial |
| RTM-31 | DEC-45 | Stage 1 — Per-dataset validation | Each of the five dataset views contains its CSV example and blocking orange validation result; the standalone CSV/XLSX validation page and navigation are absent. | Must | Di | Benyu | Valid and invalid fixtures exercise the same shared validator from each data view. Invalid files display an orange blocking result next to the upload and cannot become active. The Utilities/navigation surface contains no standalone validation route. Lin implements final view placement. | Shared CSV/XLSX checkpoint helpers and a standalone sandbox exist, so reuse is possible but placement conflicts with the latest request.<br>Planned: five view-level server tests and no-route UI test. | Group — Requirements/Data Design; Group — Implementation/Testing | Partial |

## 3. Open-item Tracking

Open items are outside the frozen `DEC-*` acceptance scope and are not counted in the RTM status summary below.

| Open ID | Decision required | Priority | Owner | Reviewer | Interim rule | Resolution criteria | Status |
|---|---|---|---|---|---|---|---|
| OPEN-02 | Confirm whether dissolved-oxygen P10 uses `9901` (% saturation) or `9924` (O2, `mg/L`). | Must | Zhaohang | Lin | Dissolved-oxygen enrichment remains `not_ready`; the two determinands must not be guessed, merged, or interchanged. | After explicit client selection, update the `DEC-24`/`DC-10` registry, fixture, and acceptance test. | Blocked |
| OPEN-03 | Closed: formal local-file format. | Should | Di | Benyu | Use five separate user-facing CSV routes; keep any XLSX support internal and historical only. | Resolved by `DEC-39`, `DEC-45`, `RTM-01`, and `RTM-31`. | Closed |
| OPEN-06 | Closed: mixed-model data sufficiency and failure handling. | Must | Lin | Di | Follow the frozen [`modelling-contract-v1.md`](modelling-contract-v1.md). | `MC-O01`–`MC-O11` are frozen; implementation remains tracked by RTM-08B/09/10/21. | Closed |
| OPEN-08 | Confirm the final Stage 5 model engine, GAM families/smooths/interactions, random-effect representation, diagnostics, comparison outputs, and export contract. | Must | Lin | Di | Treat existing `lm`/`lmer` helpers as provisional; do not claim final modelling verification or silently infer `mgcv` defaults. Predictor eligibility still follows DEC-40. | Reconcile `SRC-09` with the existing modelling contract, obtain client/scientific review, and add engine-specific acceptance and parity evidence. | Blocked |
| OPEN-13 | Freeze the in-app Biology-to-Flow/WQ/RHS identifier-mapping interaction for the five-CSV standard. | Must | Di | Benyu | Keep mapping explicit and separate from the five CSV schemas; never infer it from equal identifier text. Lin owns the UI interaction. | Define creation, validation, revision, persistence, download, and stale-propagation behaviour and add Stage 1 mapping tests. | Open |

## 4. RTM Status Summary

| Status | Count | Meaning |
|---|---:|---|
| `Verified` | 0 | No new decision yet has both a complete implementation and complete acceptance evidence. |
| `Partial` | 19 | A legacy implementation, partial helper, prototype, or historical test exists but does not yet provide complete acceptance evidence. |
| `Blocked` | 2 | The two final modelling-path requirements await resolution of `OPEN-08`; existing model helpers remain provisional evidence. |
| `Not started` | 11 | No matching implementation or reproducible acceptance evidence was found. |

## 5. Week 7 Review Gate

The matrix may be frozen only when all of the following conditions are met:

1. Every baseline decision from `DEC-01` through `DEC-25` and every controlled decision through `DEC-48` is covered by at least one RTM row or an explicit supersession reference.
2. Owners and reviewers match the Week 7 role allocation.
3. Every acceptance criterion supports an objective pass/fail decision through an automated or reproducible manual test.
4. Every `Partial` item cites current evidence and states the remaining gap.
5. Subsequent issues link both the RTM ID and DEC ID; status changes to `Verified` only after PR/commit/test evidence is attached.
